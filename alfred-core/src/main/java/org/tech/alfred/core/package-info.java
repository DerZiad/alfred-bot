/**
 * Alfred core: framework-agnostic domain model and SPIs.
 *
 * <p>This package and its sub-packages must not depend on Spring, JavaFX,
 * Ollama, Whisper, Piper, SQLite, or any other concrete provider. Adapters
 * live in their own modules (alfred-ai, alfred-audio, alfred-memory, ...).
 */
package org.tech.alfred.core;
